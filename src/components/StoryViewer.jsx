// TODO: 実装予定
// - ページ送り（前へ/次へ、アニメーションなしで一瞬で切り替え。読み上げ中でも送れる）
// - 画像表示（未配置ならプレースホルダー表示）
// - 日本語・タイ語それぞれ独立した表示オン/オフ、独立した音声再生/停止ボタン（ファイルが無ければ非表示）
// - 本文中の重要単語の太字タップ表示（タップで詳細カードを開く。詳細は「暮らし」「ことば」「価値観」の3カテゴリ固定。閉じ方は未定なので×ボタン/外側タップ両方に対応できる作りにする）
// - 最後のページまで読み終えたら自動でReview（振り返り）画面へ遷移

function StoryViewer({ story }) {
  return (
    <div className="story-viewer">
      <p>StoryViewer（未実装）: {story?.title}</p>
    </div>
  )
}

export default StoryViewer
